# CLI docs

Available pool-creation CLI commands:

- [`create-pool`](./create-pool.md): create a balancer (weighted) liquidity pool from a JSON pool file defining the asset weights and initial reserves.
- [`create-lbp-pool`](./create-lbp-pool.md): create a liquidity bootstrapping pool whose weights shift linearly from the initial weights to the target weights over a set duration.
